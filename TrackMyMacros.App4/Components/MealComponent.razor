@using System.Diagnostics
@foreach( var foodAmount in @Meal.FoodAmounts)
{
    Debug.WriteLine(foodAmount.Quantity);
    <MealFoodItemComponent @Quantity="@foodAmount.Quantity" @FoodId="foodAmount"></MealFoodItemComponent>
}